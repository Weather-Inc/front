import { makeStyles } from '@material-ui/core/styles'

const forecastChartStyles = makeStyles(theme => ({
  card: {
    marginTop: '20px'
  },

  cardContent: {
    textAlign: 'center'
  }
}))

export default forecastChartStyles
